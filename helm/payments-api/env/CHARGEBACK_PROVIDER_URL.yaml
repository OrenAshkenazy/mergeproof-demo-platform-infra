apiVersion: v1
kind: ConfigMap
metadata:
  name: payments-api-chargeback-provider-url
data:
  # Default value for non-prod/local use. Override via helm/payments-api/values.yaml or env-specific runtime ConfigMap.
  CHARGEBACK_PROVIDER_URL: "https://chargebacks.provider.internal"
